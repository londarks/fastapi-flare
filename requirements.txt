fastapi[standard]
uvicorn[standard]
redis>=5.0.0
pydantic>=2.4.0
pydantic-settings>=2.0.0
starlette>=0.27.0
scalar-fastapi>=1.6.2

# ── Optional: Zitadel OAuth2 JWT authentication ─────────────────────────────
# Needed only when using zitadel_domain / make_zitadel_dependency.
# Install via: pip install "fastapi-flare[auth]"
# or uncomment the two lines below:
# httpx>=0.27.0
# python-jose[cryptography]>=3.3.0
# ── Optional: SQLite storage backend ───────────────────────────────────
# Needed only when using storage_backend="sqlite".
# Install via: pip install "fastapi-flare[sqlite]"
# or uncomment the line below:
# aiosqlite>=0.19.0